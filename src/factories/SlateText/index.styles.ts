import { stylesheet } from 'typestyle'

export const styles = stylesheet({
  NavItem: {
    fontSize: '.8rem',
    padding: '.5rem',
    maxHeight: '100%',
  },
  Block: {
    padding: '.5rem .5rem',
  },
  CardTitle: {
    fontSize: '1.2rem',
    fontWeight: 'bold',
    padding: '.5rem',
    overflow: 'auto',
    width: '100%',
  },
})
